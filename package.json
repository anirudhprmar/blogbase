{
  "name": "blogbase",
  "version": "0.2.0",
  "description": "Internal link analyzer and MCP server for MDX blogs",
  "license": "MIT",
  "bin": {
    "blogbase": "./cli/index.ts",
    "blogbase-mcp": "./mcp/index.ts"
  },
  "files": [
    "cli",
    "core",
    "mcp"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "prepublishOnly": "bun test && bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "commander": "^15.0.0",
    "gray-matter": "^4.0.3",
    "picocolors": "^1.1.1"
  }
}
